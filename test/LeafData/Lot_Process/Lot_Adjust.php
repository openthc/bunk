<?php
/**
 * Test the Lot Adjsut Dates
 */

namespace OpenTHC\Bunk\Test\LeafData\Lot_Process;

class Lot_Adjust extends \OpenTHC\Bunk\Test\LeafData_Test
{
	/*
			$mod = array(
			'global_inventory_id' => $I['guid'],
			//'global_adjusted_by_user_id' => $_SESSION['Contact']['guid'],
			'qty' => $qty_adj,
			'uom' => $uom_adj,
			'reason' => 'reconciliation',
			'memo' => 'Adjustment Test', // 'Closing out this finished Lot, LeafData still reporting the wrong value',
			'adjusted_at' => date('m/d/Y H:i:s a'),
		);
		var_dump($mod);
		$res = $cre->inventory_adjustment()->create($mod);
		var_dump($res);
		exit;
	*/
}
